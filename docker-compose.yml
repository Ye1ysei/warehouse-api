services:
  app:
    build: .
    ports:
      - "5000:8080"
    environment:
      - ConnectionStrings__Redis=redis:6379
    depends_on:
      - redis

  redis:
    image: redis:latest
    ports:
      - "6379:6379"